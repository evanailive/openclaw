---
description: OpenClaw 框架开发规则 - 模型提供商、安全和代码风格
alwaysApply: true
---

# OpenClaw 框架开发规则

## 添加模型提供商

1. **定义位置**: `src/agents/models-config.providers.ts`
2. **环境变量**: `src/agents/model-auth.ts`

### 必需步骤

```typescript
// 1. 定义常量
const PROVIDER_BASE_URL = "https://api.example.com/v1";
const PROVIDER_COST = { input: 0.10, output: 0.30, cacheRead: 0.05, cacheWrite: 0.08 };

// 2. 创建构建函数
function buildNewProvider(): ProviderConfig {
  return {
    baseUrl: PROVIDER_BASE_URL,
    api: "openai-completions",  // 或 "anthropic-messages"
    models: [{ id: "model-id", name: "Display Name", reasoning: false, 
               input: ["text"], cost: PROVIDER_COST, contextWindow: 128000, maxTokens: 8192 }],
  };
}

// 3. 在 resolveImplicitProviders() 中注册
const key = resolveEnvApiKeyVarName("provider") ?? 
            resolveApiKeyFromProfiles({ provider: "provider", store: authStore });
if (key) { providers.provider = { ...buildNewProvider(), apiKey: key }; }
```

## 安全规则（强制）

| 规则 | 要求 |
|------|------|
| 目录权限 | 0o700 |
| 敏感文件权限 | 0o600 |
| 敏感数据位置 | ~/.openclaw/（不在项目内） |
| 文件保存 | 使用 saveJsonFile() |
| 日志 | 使用 redactSensitiveText() 脱敏 |
| 并发写入 | 使用 proper-lockfile |

### 必须在 .gitignore 中

.env, *.key, *.pem, auth-profiles.json, credentials/

## 代码风格

- **文件命名**: kebab-case (如 model-auth.ts)
- **导入扩展名**: 使用 .js (如 import { x } from "./utils.js")
- **类型**: 优先使用 type 而非 interface
- **测试**: 同目录，.test.ts 后缀

## Commit 格式

type(scope): description

Type: feat, fix, docs, refactor, test, chore
